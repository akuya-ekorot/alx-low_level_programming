type Wall = "#";

function walk(maze: string[], wall: Wall, curr: Point, end: Point): boolean {
  //1. Base case
  // off the map
  
}

export default function solve(maze: string[], wall: Wall, start: Point, end: Point): Point[] {
  
}
